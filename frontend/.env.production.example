# Production Environment Variables
# =================================

# Supabase Configuration (REQUIRED)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# WICHTIG: NIEMALS den Service-Role-Key im Frontend verwenden!
# Der ANON-Key ist öffentlich sichtbar und das ist OK - er wird durch RLS geschützt

# Security Settings (automatisch in Production)
NODE_ENV=production

# ⚠️ Sicherheitshinweise:
# 1. Niemals sensible Keys committen
# 2. Verwende nur den ANON-Key im Frontend
# 3. Service-Role-Key NUR im Backend/Worker
# 4. Production-Builds minimieren automatisch Logs
# 5. CSP-Headers werden automatisch gesetzt

# Vercel Deployment (automatisch gesetzt)
# VERCEL=1
# VERCEL_ENV=production

