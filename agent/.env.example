# Supabase Configuration
SUPABASE_URL=https://ethwkzwsxkhcexibuvwp.supabase.co

# ⚠️ WICHTIG: Token wird über UI generiert!
# Kopiere den Token aus dem Frontend nach dem Erstellen des Agents
CONNECTOR_TOKEN=cwt_your-token-will-be-generated-in-ui

# Optional: Connector Name (wird im UI festgelegt, aber kann überschrieben werden)
CONNECTOR_NAME=my-intranet-agent

# Scan Configuration (wird im UI festgelegt, aber kann überschrieben werden)
SCAN_TARGETS=google.com,github.com
SCAN_PORTS=443,8443,636

# Scan interval in seconds (default: 3600 = 1 hour)
SCAN_INTERVAL=3600

# Scan timeout in seconds (default: 5)
SCAN_TIMEOUT=5

# Health Check Configuration
HEALTH_CHECK_PORT=8080

# Logging
# Options: DEBUG, INFO, WARN, ERROR
LOG_LEVEL=INFO
